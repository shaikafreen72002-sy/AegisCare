name: aegiscare
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - nodejs=20
  - pip:
      - pydantic>=2.0.0
      - fastapi>=0.100.0
      - uvicorn>=0.22.0
      - mistralai>=1.0.0
      - python-dotenv>=1.0.0
      - requests>=2.31.0
